const { User } = require("./user");
const { Book } = require("./book");
const { Training } = require("./training");
const {Collection} = require("./collection")

module.exports = { User, Book, Training, Collection };
